#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int a = 48;
	int line = 0;

	while (a <= 57)
	{
		putchar(a);
		a++;
	}
	putchar(line);

	return (0);
}
